# Host-side conversion environment for hand_det_pose_convert.
# Python 3.10. Install order matters: rknn-toolkit2 wheel first (it pins
# tight ranges on numpy/protobuf), then onnx must be downgraded to 1.16.1
# because 1.17+ removed onnx.mapping which rknn-toolkit2 still imports.
#
# The rknn-toolkit2 wheel itself is NOT on PyPI; install it from the
# bundled submodule, e.g.:
#   pip install third_party/rknn-toolkit2/rknn-toolkit2/packages/x86_64/cp310/rknn_toolkit2-2.3.2-cp310-cp310-linux_x86_64.whl

onnx==1.16.1
onnxruntime==1.18.1
numpy==1.26.4
opencv-python==4.10.0.84
protobuf==4.25.3
